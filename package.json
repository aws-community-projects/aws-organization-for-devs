{
    "name": "aws-organization-for-devs",
    "version": "0.1.0",
    "scripts": {
        "build": "vite build",
        "clean": "rimraf cdk.out coverage dist",
        "deploy": "npm run clean && cdk deploy --all --require-approval never",
        "destroy": "cdk destroy",
        "diff": "cdk diff",
        "synth": "cdk synth",
        "test": "vitest run --coverage --silent"
    },
    "dependencies": {
        "aws-cdk-github-oidc": "^2.3.2",
        "aws-cdk-lib": "^2.80.0",
        "constructs": "^10.2.3"
    },
    "devDependencies": {
        "@types/node": "^18.15.13",
        "@vitest/coverage-c8": "^0.30.1",
        "aws-cdk": "^2.76.0",
        "rimraf": "^5.0.0",
        "ts-node": "^10.9.1",
        "typescript": "^5.0.4",
        "vitest": "^0.30.1"
    }
}
